/**
 * @travelmate/ui — shared presentational components. Pure rendering: props in,
 * markup out. No fetching, no LLM, no Database writes (P5). Imports only
 * @travelmate/contracts (for prop types). RN-safe so apps/mobile reuses it.
 */
export { ItineraryView } from "./ItineraryView.js";
export { TripDescribe } from "./TripDescribe.js";
