/**
 * Home. Skeleton placeholder — the real screen mounts <TripDescribe/> then
 * subscribes to the plan via the Database Observer and renders <ItineraryView/>.
 * The UX never fetches or calls the LLM (P5).
 */
export default function Home() {
  return (
    <main>
      <h1>TravelMate</h1>
      <p>Skeleton. See about_travelMate.md and projectStructure.md.</p>
    </main>
  );
}
